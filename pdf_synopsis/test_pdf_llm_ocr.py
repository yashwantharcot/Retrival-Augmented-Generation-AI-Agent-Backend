import pytest

# This file is a manual OCR + LLM synopsis script and depends on local binaries and API keys.
# Skip at module level to keep CI/pytest runs clean.
pytest.skip("Skipping manual OCR+LLM test; requires local OCR and API setup.", allow_module_level=True)





